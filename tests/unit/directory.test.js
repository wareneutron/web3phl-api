const { expect } = require("chai");

// Directory Tests
describe('Test /directory', () => {
  describe('Placeholder test', () => {
    it ('should pass', () => {
      expect(true).to.equal(true);
    });
  });
});
